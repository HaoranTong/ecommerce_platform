# 模块详细设计任务模板

完成[product-catalog]模块的详细设计任务，该模块目录已经存在，你检查文档是否齐全，文档内容是否完整和正确，补充完善缺失的内容，修改其中的错误，以下为你必须执行的步骤和必须完成的任务内容已经参考上下文

## 全局上下文
- 项目架构: FastAPI 0.104.1 + SQLAlchemy 2.0.23 + Pydantic 2.5.0
- 架构模式: 模块化单体 (Router→Service→Model)
- 数据库: MySQL 8.0
- 缓存: Redis 7.0
- Python版本: 3.10
- 部署方式: Docker Compose

## 上下文变量 (YAML)
在开始任务前，请提取并记录以下变量，以便在整个设计过程中引用：
```yaml
product-catalog: [模块名]
business_domain: [业务域]
core_entities:
  - [实体1]
  - [实体2]
functional_scope:
  includes:
    - [功能1]
  excludes:
    - [功能2]
dependencies:
  depends_on:
    - [模块]: [原因]
  depended_by:
    - [模块]: [场景]
standards:
  document_structure: A8(8章节), A10(7章节)
  naming_conventions: [naming-conventions-standards.md]
  api_principles: [api-standards.md]
```

## 输入参数
- 模块名：`[product-catalog]`（技术实现名，如product_catalog）

## 执行前置条件

### 必读文档（按序执行）
1. `docs/architecture/business-architecture.md` 第1-200行
   - 操作：搜索模块名，定位业务域和边界定义
   - 提取：包含功能、排除功能、核心实体、业务规则

2. `docs/architecture/dependency-architecture.md` 第1-200行
   - 提取：依赖关系、依赖方向、数据流向

3. `docs/standards/document-management-standards.md` 第300-350行
   - 提取：A8标准8章节结构

4. `docs/standards/naming-conventions-standards.md` 第1-100行
   - 提取：模块命名映射（技术名、API路径、表前缀）

5. `docs/standards/api-standards.md` 第1-100行
   - 提取：RESTful设计原则、HTTP方法规范

6. `docs/standards/database-standards.md` 第1-100行
   - 提取：主键设计、外键约束、索引规范

### 边界分析（强制步骤）
创建临时文件记录：
```yaml
module: [product-catalog]
business_domain: [从6大域选择]
included_functions:
  - [功能1]: [职责说明]
excluded_functions:
  - [功能1]: [由XX模块负责]
depends_on:
  - [模块名]: [原因]
depended_by:
  - [模块名]: [场景]
```

## 执行步骤

### 步骤1：检查现有文档
```bash
ls -la docs/design/modules/[product-catalog]/
```

### 步骤2：创建7个强制文档

#### 2.1 README.md (A3标准)
- 模块简介（1段）
- 导航链接（指向其他6个文档）

#### 2.2 overview.md (A6标准，6章节)
1. 模块概述
2. 主要职责
3. 业务价值
4. 模块边界（包含/排除/依赖）
5. 技术架构
6. 相关文档

#### 2.3 requirements.md
- REQ-[module]-[id]格式需求列表
- 验收标准

#### 2.4 design.md (A8标准，8章节)
1. **引言**
   - 业务域定位
   - 模块边界（引用business-architecture.md）
   - 功能范围（包含/排除）
   - 依赖关系（引用dependency-architecture.md）
   - REQ编号引用
   - 架构约束

2. **设计概览**
   - Mermaid架构图（Router→Service→Model）
   - 层次职责
   - 与core组件依赖（database/auth/redis_client）

3. **数据模型**
   - 表结构（SQL DDL）
   - 字段定义
   - 外键关系
   - 索引设计

4. **业务流程**
   - 核心流程图（Mermaid时序图）
   - 状态机
   - 异常处理

5. **接口设计**
   - API端点表（方法/路径/功能/请求/响应）
   - 认证授权
   - 错误码

6. **安全考虑**
   - 认证机制
   - 数据保护
   - 风险控制

7. **性能考量**
   - 量化指标（响应时间/并发数/QPS）
   - 优化策略
   - 监控指标

8. **变更影响**
   - 兼容性分析
   - 迁移方案
   - 影响范围

#### 2.5 api-spec.md (A10标准，7章节)
按A10标准编写API规范

#### 2.6 api-implementation.md
记录API实际实现与规范的对应关系

#### 2.7 implementation.md
记录代码实现细节和技术决策

### 步骤3：边界验证（自检）

```yaml
data_model_check:
  - 所有字段属于本模块: [true/false]
  - 无其他模块字段: [true/false]
  - 外键符合依赖: [true/false]

business_logic_check:
  - 逻辑在职责范围: [true/false]
  - 无跨模块实现: [true/false]
  - 跨模块用接口: [true/false]

api_check:
  - 端点职责一致: [true/false]
  - 无暴露他模块: [true/false]
  - 响应仅本模块: [true/false]

dependency_check:
  - 依赖方向正确: [true/false]
  - 无反向依赖: [true/false]
  - 无循环依赖: [true/false]
```

### 步骤4：标准验证
```bash
tools/validate_standards.ps1 -Action full -DocPath docs/design/modules/[product-catalog]/design.md
tools/check_naming_compliance.ps1 -ModuleName [product-catalog]
```

## 禁止行为（触发失败）

### 模块边界违规
- ❌ 数据模型包含其他模块字段
- ❌ Service直接操作其他模块表
- ❌ API暴露其他模块职责
- ❌ 反向依赖或循环依赖

### 技术标准违规
❌ Pydantic V1语法
❌ 省略design.md任何章节
❌ 非RESTful API设计
❌ 违反命名规范

## 错误处理流程

在以下任一步骤检测到异常时，请立即停止执行，输出错误信息，并等待人工干预：

1. 文档读取失败
   - 条件：指定路径的文档不存在或无法读取
   - 操作：输出 "文档 [file_path] 未找到或无法读取"，终止

2. 模块定义缺失
   - 条件：在 `business-architecture.md` 中搜索 `product-catalog` 未找到
   - 操作：输出 "未找到模块定义 [product-catalog]，请检查业务架构文档"，终止

3. 工具执行失败
   - 条件：校验工具（如 `validate_standards.ps1`）返回非零退出码
   - 操作：输出 "工具 [tool_name] 执行失败，错误信息: [error_message]"，终止

4. 依赖提取失败
   - 条件：`dependency-architecture.md` 无对应模块依赖信息
   - 操作：输出 "依赖信息提取失败: 模块 [product-catalog] 在 dependency-architecture.md 中未定义"，终止

## 输出验收

### 文档完整性
- [ ] 7个文档全部创建
- [ ] 文档间交叉引用正确
- [ ] 元数据完整

### 技术规范性
- [ ] 命名符合naming-conventions-standards.md
- [ ] 数据库设计符合database-standards.md
- [ ] API设计符合api-standards.md
- [ ] Pydantic 2.0语法
- [ ] Docker Compose兼容

### 边界合规性
- [ ] 业务边界引用正确
- [ ] 依赖关系符合架构
- [ ] 无跨模块数据访问
- [ ] 边界自检全部通过

### 工具验证
- [ ] validate_standards.ps1通过
- [ ] check_naming_compliance.ps1通过

## 问题处理索引

| 问题类型 | 查找位置 | 操作 |
|---------|---------|------|
| 边界不清 | business-architecture.md | 搜索模块名，查看定义 |
| 依赖不明 | dependency-architecture.md | 查看依赖图 |
| 命名冲突 | naming-conventions-standards.md | 查映射表 |
| 标准不明 | document-management-standards.md | 搜索A3/A6/A8/A10 |

## 输出格式

完成后提供：
1. 7个文档的路径列表
2. 边界自检结果（YAML格式）
3. 工具验证结果
4. 关键设计决策摘要（3-5条）

## 任务描述
- 目标：基于输入参数和全局上下文，生成符合系统架构与技术规范的模块详细设计文档
- 范围：产出7份文档（README、overview、requirements、design、api-spec、api-implementation、implementation），并提供边界自检与工具校验结果
- 要求：
  - 遵循模块化单体架构（Router→Service→Model）
  - 严格符合A3/A6/A8/A10文档标准
  - 文档中必须注入核心上下文变量与依赖信息
  - 执行错误处理流程，遇异常时停止并报告

## 期望输出
1. docs/design/modules/[product-catalog]/README.md
2. docs/design/modules/[product-catalog]/overview.md
3. docs/design/modules/[product-catalog]/requirements.md
4. docs/design/modules/[product-catalog]/design.md
5. docs/design/modules/[product-catalog]/api-spec.md
6. docs/design/modules/[product-catalog]/api-implementation.md
7. docs/design/modules/[product-catalog]/implementation.md
8. 边界自检结果（YAML）
9. 工具校验结果（日志或摘要）
