# Auto-generated test file
# Generated at: 20251010_172257
# Target path: tests/unit/test_user_auth_repositories.py
# Do NOT edit this file manually - regenerate from source models

"""
Auto Generated Test - Repository Layer

文件路径: tests/unit/test_repositories/test_user_auth_repositories.py
生成时间: 2025-10-10 17:22:56
生成工具: tools/generate_test_template.py v3.0
模块: user_auth

测试类型: 单元测试 - Repository数据访问层
测试策略: SQLite内存数据库
测试重点: 
- CRUD操作正确性
- 查询条件和过滤逻辑
- 事务提交和回滚
- 边界情况和错误处理

符合标准: 
- testing-standards.md - 四层架构测试策略
- architecture/overview.md - Repository模式标准

[CHECK:TEST-001] [CHECK:DEV-009]
"""

import pytest
from typing import List, Optional
from sqlalchemy.orm import Session
from sqlalchemy.exc import IntegrityError
from datetime import datetime, timedelta
from decimal import Decimal

# 导入测试基础设施
from tests.conftest import unit_test_db

# 导入Repository类
from app.modules.user_auth.repository import (
    UserRepository, RoleRepository, PermissionRepository, UserRoleRepository, RolePermissionRepository, SessionRepository
)

# 导入模型类
from app.modules.user_auth.models import (
    Permission, Role, RolePermission, Session, User, UserRole
)


@pytest.mark.unit
@pytest.mark.repositories
class TestUserRepository:
    """
    UserRepository 数据访问层测试
    
    测试范围:
    - 15 个Repository方法
    - CRUD操作完整性
    - 查询逻辑正确性
    - 事务处理和数据一致性
    
    测试策略: SQLite内存数据库 + Factory Boy
    """
        
    def test_create_minimal_fields(self, unit_test_db: Session):
        """测试create - 最小必填字段创建
        
        符合标准: testing-standards.md 第2.1节 - 只填写必填字段，验证默认值
        数据准备策略: 最小实体构造，不使用Factory Boy
        """
# 创建最小实体（只填必填字段）
        entity = User(
            username="username",
            email="email",
            password_hash="password_hash",
            is_active=False,
            status="status",
            email_verified=False,
            phone_verified=False,
            two_factor_enabled=False,
            failed_login_attempts=1,
            role="role"
        )
        
        # 执行Repository方法
        result = UserRepository.create(unit_test_db, entity)
        
        # 验证必填字段
        assert result is not None
        assert result.id is not None  # 验证ID已生成
        
        # 验证默认值（Column(default=...)定义的值）
        # 示例: assert result.is_active == True
        # 示例: assert result.status == "active"
        # TODO: 根据实际模型补充默认值验证
        
        # 验证数据已持久化
        db_entity = unit_test_db.query(User).filter_by(id=result.id).first()
        assert db_entity is not None
    
    def test_create_full_fields(self, unit_test_db: Session):
        """测试create - 完整字段创建
        
        符合标准: testing-standards.md 第2.1节 - 填写所有字段，验证保存正确
        数据准备策略: 使用Factory Boy
        """
        # 使用Factory Boy创建完整实体
        from tests.factories.user_auth_factories import UserFactory
        entity = UserFactory.build()  # build不自动保存到数据库
        
        # 执行Repository方法
        result = UserRepository.create(unit_test_db, entity)
        
        # 验证所有字段保存正确
        assert result is not None
        assert result.id is not None
        # TODO: 验证其他字段值正确保存
        
        # 验证持久化
        db_entity = unit_test_db.query(User).filter_by(id=result.id).first()
        assert db_entity is not None
    
    def test_create_transaction_commit(self, unit_test_db: Session):
        """测试create - 事务提交验证
        
        符合标准: testing-standards.md 第2.5节 - 验证数据真正写入数据库
        """
        from tests.factories.user_auth_factories import UserFactory
        entity = UserFactory.build()
        
        result = UserRepository.create(unit_test_db, entity)
        
        # 验证事务已提交（expire后重新查询能找到）
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(User).filter_by(id=result.id).first()
        assert db_entity is not None

    def test_get_by_id_found(self, unit_test_db: Session):
        """测试get_by_id - 查询到数据"""
        # 准备依赖实体
        user = User(username="User数据", email="test_user数据@example.com", password_hash="User数据", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(user)
        unit_test_db.commit()
        
        # 准备测试数据
        entity = User(username="查询测试", email="test_查询测试@example.com", password_hash="查询测试", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = UserRepository.get_by_id(unit_test_db, user.id)
        
        # 验证结果
        assert result is not None

    def test_get_by_id_not_found(self, unit_test_db: Session):
        """测试get_by_id - 数据不存在"""
        result = UserRepository.get_by_id(unit_test_db, "nonexistent_value_12345")
        
        # 验证结果
        assert result is None

    def test_get_by_username_found(self, unit_test_db: Session):
        """测试get_by_username - 查询到数据"""
        # 准备测试数据
        entity = User(username="查询测试", email="test_查询测试@example.com", password_hash="查询测试", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = UserRepository.get_by_username(unit_test_db, entity.username)
        
        # 验证结果
        assert result is not None
        assert result.username == entity.username
    
    def test_get_by_username_not_found(self, unit_test_db: Session):
        """测试get_by_username - 数据不存在"""
        result = UserRepository.get_by_username(unit_test_db, "nonexistent_value")
        
        assert result is None

    def test_get_by_email_found(self, unit_test_db: Session):
        """测试get_by_email - 查询到数据"""
        # 准备测试数据
        entity = User(username="查询测试", email="test_查询测试@example.com", password_hash="查询测试", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = UserRepository.get_by_email(unit_test_db, entity.email)
        
        # 验证结果
        assert result is not None
        assert result.email == entity.email
    
    def test_get_by_email_not_found(self, unit_test_db: Session):
        """测试get_by_email - 数据不存在"""
        result = UserRepository.get_by_email(unit_test_db, "nonexistent_value")
        
        assert result is None

    def test_get_by_username_or_email_found(self, unit_test_db: Session):
        """测试get_by_username_or_email - 查询到数据"""
        # 准备测试数据
        entity = User(username="查询测试", email="test_查询测试@example.com", password_hash="查询测试", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = UserRepository.get_by_username_or_email(unit_test_db, entity.username)
        
        # 验证结果
        assert result is not None
        assert result.username == entity.username
    
    def test_get_by_username_or_email_not_found(self, unit_test_db: Session):
        """测试get_by_username_or_email - 数据不存在"""
        result = UserRepository.get_by_username_or_email(unit_test_db, "nonexistent_value")
        
        assert result is None

    def test_get_by_phone_found(self, unit_test_db: Session):
        """测试get_by_phone - 查询到数据"""
        # 准备测试数据
        entity = User(username="查询测试", email="test_查询测试@example.com", password_hash="查询测试", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = UserRepository.get_by_phone(unit_test_db, entity.phone)
        
        # 验证结果
        assert result is not None
        assert result.phone == entity.phone
    
    def test_get_by_phone_not_found(self, unit_test_db: Session):
        """测试get_by_phone - 数据不存在"""
        result = UserRepository.get_by_phone(unit_test_db, "nonexistent_value")
        
        assert result is None

    def test_get_by_wx_openid_found(self, unit_test_db: Session):
        """测试get_by_wx_openid - 查询到数据"""
        # 准备测试数据
        entity = User(username="查询测试", email="test_查询测试@example.com", password_hash="查询测试", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = UserRepository.get_by_wx_openid(unit_test_db, entity.wx_openid)
        
        # 验证结果
        assert result is not None
        assert result.wx_openid == entity.wx_openid
    
    def test_get_by_wx_openid_not_found(self, unit_test_db: Session):
        """测试get_by_wx_openid - 数据不存在"""
        result = UserRepository.get_by_wx_openid(unit_test_db, "nonexistent_value")
        
        assert result is None

    def test_check_exists_found(self, unit_test_db: Session):
        """测试check_exists - 查询到数据"""
        # 准备测试数据
        entity = User(username="查询测试", email="test_查询测试@example.com", password_hash="查询测试", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = UserRepository.check_exists(unit_test_db, username=entity.username, email=entity.email)
        
        # 验证结果
        assert result is True
    
    def test_check_exists_not_found(self, unit_test_db: Session):
        """测试check_exists - 数据不存在"""
        result = UserRepository.check_exists(unit_test_db, username="nonexistent", email="nonexistent@test.com")
        
        assert result is False

    def test_list_query(self, unit_test_db: Session):
        """测试list - 查询功能"""
        # 准备测试数据
        entity = User(username="查询测试", email="test_查询测试@example.com", password_hash="查询测试", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        results = UserRepository.list(unit_test_db, 0, 0, None, "", "", "")
        
        # 验证查询结果
        assert isinstance(results, list)  # 返回列表

    def test_count_query(self, unit_test_db: Session):
        """测试count - 查询功能"""
        # 准备测试数据
        entity = User(username="查询测试", email="test_查询测试@example.com", password_hash="查询测试", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = UserRepository.count(unit_test_db)
        
        # 验证查询结果
        assert result >= 0  # count方法返回int

    def test_update_single_field(self, unit_test_db: Session):
        """测试update - 单字段更新
        
        符合标准: testing-standards.md 第2.3节 - 只更新一个字段，验证其他字段不变
        """
        # 准备测试数据
        from tests.factories.user_auth_factories import UserFactory, UserAuthFactoryManager
        UserAuthFactoryManager.setup_factories(unit_test_db)
        entity = UserFactory.create()
        original_is_active = entity.is_active
        
        # 执行Repository方法（只更新real_name）
        update_data = {"real_name": "更新后数据"}
        result = UserRepository.update(unit_test_db, entity, update_data)  # TODO: 根据实际方法签名调整
        
        # 验证目标字段已更新
        assert result.real_name == "更新后数据"
        
        # ✅ 验证其他字段未变化
        assert result.is_active == original_is_active
        
        # 验证数据库已更新
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(User).filter_by(id=entity.id).first()
        assert db_entity.real_name == "更新后数据"
        assert db_entity.is_active == original_is_active
    
    def test_update_multiple_fields(self, unit_test_db: Session):
        """测试update - 多字段更新
        
        符合标准: testing-standards.md 第2.3节 - 同时更新多个字段
        """
        from tests.factories.user_auth_factories import UserFactory, UserAuthFactoryManager
        UserAuthFactoryManager.setup_factories(unit_test_db)
        entity = UserFactory.create()
        
        # 执行Repository方法（同时更新多个字段）
        update_data = {
            "real_name": "更新后数据1",
            "is_active": False
        }
        result = UserRepository.update(unit_test_db, entity, update_data)
        
        # 验证所有字段已更新
        assert result.real_name == "更新后数据1"
        assert result.is_active == False
        
        # 验证持久化
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(User).filter_by(id=entity.id).first()
        assert db_entity.real_name == "更新后数据1"
        assert db_entity.is_active == False
    
    def test_update_transaction_commit(self, unit_test_db: Session):
        """测试update - 事务提交验证
        
        符合标准: testing-standards.md 第2.5节 - 验证更新真正写入数据库
        """
        from tests.factories.user_auth_factories import UserFactory, UserAuthFactoryManager
        UserAuthFactoryManager.setup_factories(unit_test_db)
        entity = UserFactory.create()
        
        update_data = {"real_name": "事务测试数据"}
        result = UserRepository.update(unit_test_db, entity, update_data)
        
        # 验证事务已提交
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(User).filter_by(id=entity.id).first()
        assert db_entity.real_name == "事务测试数据"
    
    def test_update_specialized_method(self, unit_test_db: Session):
        """测试update - 专用方法测试（如有）
        
        符合标准: testing-standards.md 第2.3节 - 测试特殊更新方法
        示例: update_status, update_password, activate, deactivate等
        """
        # TODO: 如果有专用更新方法，在这里测试
        # 例如:
        # entity = UserFactory.create(status='active')
        # result = UserRepository.update_status(unit_test_db, entity.id, 'inactive')
        # assert result.status == 'inactive'
        pass

    # TODO: 测试专用更新方法 update_login_info
    # 这是一个专用更新方法，只修改特定字段，需要根据业务逻辑手动编写测试
    # 方法签名: [('user', 'User'), ('ip_address', 'Optional[str]')]
    # 返回类型: User
    

    def test_increment_failed_login_query(self, unit_test_db: Session):
        """测试increment_failed_login - 查询功能"""
        # 准备测试数据
        entity = User(username="查询测试", email="test_查询测试@example.com", password_hash="查询测试", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = UserRepository.increment_failed_login(unit_test_db, entity)
        
        # 验证查询结果
        assert result is not None

    def test_soft_delete_success(self, unit_test_db: Session):
        """测试soft_delete - 删除成功"""
        # 准备测试数据
        entity = User(username="待删除数据", email="test_待删除数据@example.com", password_hash="待删除数据", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(entity)
        unit_test_db.commit()
        entity_id = entity.id
        
        # 执行Repository方法（传递对象）
        UserRepository.soft_delete(unit_test_db, entity)
        
        # 验证软删除
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(User).filter_by(id=entity_id).first()
        assert db_entity is not None  # 记录仍存在
        # 验证软删除标记（根据模型字段选择）
        if hasattr(db_entity, 'is_deleted'):
            assert db_entity.is_deleted == True
        if hasattr(db_entity, 'is_active'):
            assert db_entity.is_active == False

    def test_hard_delete_success(self, unit_test_db: Session):
        """测试hard_delete - 删除成功"""
        # 准备测试数据
        entity = User(username="待删除数据", email="test_待删除数据@example.com", password_hash="待删除数据", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(entity)
        unit_test_db.commit()
        entity_id = entity.id
        
        # 执行Repository方法（传递对象）
        UserRepository.hard_delete(unit_test_db, entity)
        
        # 验证硬删除
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(User).filter_by(id=entity_id).first()
        assert db_entity is None  # 记录已物理删除




@pytest.mark.unit
@pytest.mark.repositories
class TestRoleRepository:
    """
    RoleRepository 数据访问层测试
    
    测试范围:
    - 7 个Repository方法
    - CRUD操作完整性
    - 查询逻辑正确性
    - 事务处理和数据一致性
    
    测试策略: SQLite内存数据库 + Factory Boy
    """
        
    def test_create_minimal_fields(self, unit_test_db: Session):
        """测试create - 最小必填字段创建
        
        符合标准: testing-standards.md 第2.1节 - 只填写必填字段，验证默认值
        数据准备策略: 最小实体构造，不使用Factory Boy
        """
# 创建最小实体（只填必填字段）
        entity = Role(
            name="name",
            level=1
        )
        
        # 执行Repository方法
        result = RoleRepository.create(unit_test_db, entity)
        
        # 验证必填字段
        assert result is not None
        assert result.id is not None  # 验证ID已生成
        
        # 验证默认值（Column(default=...)定义的值）
        # 示例: assert result.is_active == True
        # 示例: assert result.status == "active"
        # TODO: 根据实际模型补充默认值验证
        
        # 验证数据已持久化
        db_entity = unit_test_db.query(Role).filter_by(id=result.id).first()
        assert db_entity is not None
    
    def test_create_full_fields(self, unit_test_db: Session):
        """测试create - 完整字段创建
        
        符合标准: testing-standards.md 第2.1节 - 填写所有字段，验证保存正确
        数据准备策略: 使用Factory Boy
        """
        # 使用Factory Boy创建完整实体
        from tests.factories.user_auth_factories import RoleFactory
        entity = RoleFactory.build()  # build不自动保存到数据库
        
        # 执行Repository方法
        result = RoleRepository.create(unit_test_db, entity)
        
        # 验证所有字段保存正确
        assert result is not None
        assert result.id is not None
        # TODO: 验证其他字段值正确保存
        
        # 验证持久化
        db_entity = unit_test_db.query(Role).filter_by(id=result.id).first()
        assert db_entity is not None
    
    def test_create_transaction_commit(self, unit_test_db: Session):
        """测试create - 事务提交验证
        
        符合标准: testing-standards.md 第2.5节 - 验证数据真正写入数据库
        """
        from tests.factories.user_auth_factories import RoleFactory
        entity = RoleFactory.build()
        
        result = RoleRepository.create(unit_test_db, entity)
        
        # 验证事务已提交（expire后重新查询能找到）
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(Role).filter_by(id=result.id).first()
        assert db_entity is not None

    def test_get_by_id_found(self, unit_test_db: Session):
        """测试get_by_id - 查询到数据"""
        # 准备依赖实体
        role = Role(name="Role数据", level=1)
        unit_test_db.add(role)
        unit_test_db.commit()
        
        # 准备测试数据
        entity = Role(name="查询测试", level=1)
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = RoleRepository.get_by_id(unit_test_db, role.id)
        
        # 验证结果
        assert result is not None

    def test_get_by_id_not_found(self, unit_test_db: Session):
        """测试get_by_id - 数据不存在"""
        result = RoleRepository.get_by_id(unit_test_db, "nonexistent_value_12345")
        
        # 验证结果
        assert result is None

    def test_get_by_name_found(self, unit_test_db: Session):
        """测试get_by_name - 查询到数据"""
        # 准备测试数据
        entity = Role(name="查询测试", level=1)
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = RoleRepository.get_by_name(unit_test_db, entity.name)
        
        # 验证结果
        assert result is not None
        assert result.name == entity.name
    
    def test_get_by_name_not_found(self, unit_test_db: Session):
        """测试get_by_name - 数据不存在"""
        result = RoleRepository.get_by_name(unit_test_db, "nonexistent_value")
        
        assert result is None

    def test_list_query(self, unit_test_db: Session):
        """测试list - 查询功能"""
        # 准备测试数据
        entity = Role(name="查询测试", level=1)
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        results = RoleRepository.list(unit_test_db, 0, 0)
        
        # 验证查询结果
        assert isinstance(results, list)  # 返回列表

    def test_update_single_field(self, unit_test_db: Session):
        """测试update - 单字段更新
        
        符合标准: testing-standards.md 第2.3节 - 只更新一个字段，验证其他字段不变
        """
        # 准备测试数据
        from tests.factories.user_auth_factories import RoleFactory, UserAuthFactoryManager
        UserAuthFactoryManager.setup_factories(unit_test_db)
        entity = RoleFactory.create()
        original_description = entity.description
        
        # 执行Repository方法（只更新name）
        update_data = {"name": "更新后数据"}
        result = RoleRepository.update(unit_test_db, entity, update_data)  # TODO: 根据实际方法签名调整
        
        # 验证目标字段已更新
        assert result.name == "更新后数据"
        
        # ✅ 验证其他字段未变化
        assert result.description == original_description
        
        # 验证数据库已更新
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(Role).filter_by(id=entity.id).first()
        assert db_entity.name == "更新后数据"
        assert db_entity.description == original_description
    
    def test_update_multiple_fields(self, unit_test_db: Session):
        """测试update - 多字段更新
        
        符合标准: testing-standards.md 第2.3节 - 同时更新多个字段
        """
        from tests.factories.user_auth_factories import RoleFactory, UserAuthFactoryManager
        UserAuthFactoryManager.setup_factories(unit_test_db)
        entity = RoleFactory.create()
        
        # 执行Repository方法（同时更新多个字段）
        update_data = {
            "name": "更新后数据1",
            "description": "更新后数据2"
        }
        result = RoleRepository.update(unit_test_db, entity, update_data)
        
        # 验证所有字段已更新
        assert result.name == "更新后数据1"
        assert result.description == "更新后数据2"
        
        # 验证持久化
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(Role).filter_by(id=entity.id).first()
        assert db_entity.name == "更新后数据1"
        assert db_entity.description == "更新后数据2"
    
    def test_update_transaction_commit(self, unit_test_db: Session):
        """测试update - 事务提交验证
        
        符合标准: testing-standards.md 第2.5节 - 验证更新真正写入数据库
        """
        from tests.factories.user_auth_factories import RoleFactory, UserAuthFactoryManager
        UserAuthFactoryManager.setup_factories(unit_test_db)
        entity = RoleFactory.create()
        
        update_data = {"name": "事务测试数据"}
        result = RoleRepository.update(unit_test_db, entity, update_data)
        
        # 验证事务已提交
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(Role).filter_by(id=entity.id).first()
        assert db_entity.name == "事务测试数据"
    
    def test_update_specialized_method(self, unit_test_db: Session):
        """测试update - 专用方法测试（如有）
        
        符合标准: testing-standards.md 第2.3节 - 测试特殊更新方法
        示例: update_status, update_password, activate, deactivate等
        """
        # TODO: 如果有专用更新方法，在这里测试
        # 例如:
        # entity = RoleFactory.create(status='active')
        # result = RoleRepository.update_status(unit_test_db, entity.id, 'inactive')
        # assert result.status == 'inactive'
        pass

    def test_delete_success(self, unit_test_db: Session):
        """测试delete - 删除成功"""
        # 准备测试数据
        entity = Role(name="待删除数据", level=1)
        unit_test_db.add(entity)
        unit_test_db.commit()
        entity_id = entity.id
        
        # 执行Repository方法（传递对象）
        RoleRepository.delete(unit_test_db, entity)
        
        # 验证硬删除
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(Role).filter_by(id=entity_id).first()
        assert db_entity is None  # 记录已物理删除

    def test_get_user_count_found(self, unit_test_db: Session):
        """测试get_user_count - 查询到数据"""
        # 准备依赖实体
        role = Role(name="Role数据", level=1)
        unit_test_db.add(role)
        unit_test_db.commit()
        
        # 准备测试数据
        entity = Role(name="查询测试", level=1)
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = RoleRepository.get_user_count(unit_test_db, role.id)
        
        # 验证结果
        assert result is not None

    def test_get_user_count_not_found(self, unit_test_db: Session):
        """测试get_user_count - 数据不存在"""
        result = RoleRepository.get_user_count(unit_test_db, "nonexistent_value_12345")
        
        # 验证结果
        assert result == 0  # count方法返回0




@pytest.mark.unit
@pytest.mark.repositories
class TestPermissionRepository:
    """
    PermissionRepository 数据访问层测试
    
    测试范围:
    - 6 个Repository方法
    - CRUD操作完整性
    - 查询逻辑正确性
    - 事务处理和数据一致性
    
    测试策略: SQLite内存数据库 + Factory Boy
    """
        
    def test_create_minimal_fields(self, unit_test_db: Session):
        """测试create - 最小必填字段创建
        
        符合标准: testing-standards.md 第2.1节 - 只填写必填字段，验证默认值
        数据准备策略: 最小实体构造，不使用Factory Boy
        """
# 创建最小实体（只填必填字段）
        entity = Permission(
            name="name",
            resource="resource",
            action="action"
        )
        
        # 执行Repository方法
        result = PermissionRepository.create(unit_test_db, entity)
        
        # 验证必填字段
        assert result is not None
        assert result.id is not None  # 验证ID已生成
        
        # 验证默认值（Column(default=...)定义的值）
        # 示例: assert result.is_active == True
        # 示例: assert result.status == "active"
        # TODO: 根据实际模型补充默认值验证
        
        # 验证数据已持久化
        db_entity = unit_test_db.query(Permission).filter_by(id=result.id).first()
        assert db_entity is not None
    
    def test_create_full_fields(self, unit_test_db: Session):
        """测试create - 完整字段创建
        
        符合标准: testing-standards.md 第2.1节 - 填写所有字段，验证保存正确
        数据准备策略: 使用Factory Boy
        """
        # 使用Factory Boy创建完整实体
        from tests.factories.user_auth_factories import PermissionFactory
        entity = PermissionFactory.build()  # build不自动保存到数据库
        
        # 执行Repository方法
        result = PermissionRepository.create(unit_test_db, entity)
        
        # 验证所有字段保存正确
        assert result is not None
        assert result.id is not None
        # TODO: 验证其他字段值正确保存
        
        # 验证持久化
        db_entity = unit_test_db.query(Permission).filter_by(id=result.id).first()
        assert db_entity is not None
    
    def test_create_transaction_commit(self, unit_test_db: Session):
        """测试create - 事务提交验证
        
        符合标准: testing-standards.md 第2.5节 - 验证数据真正写入数据库
        """
        from tests.factories.user_auth_factories import PermissionFactory
        entity = PermissionFactory.build()
        
        result = PermissionRepository.create(unit_test_db, entity)
        
        # 验证事务已提交（expire后重新查询能找到）
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(Permission).filter_by(id=result.id).first()
        assert db_entity is not None

    def test_get_by_id_found(self, unit_test_db: Session):
        """测试get_by_id - 查询到数据"""
        # 准备依赖实体
        permission = Permission(name="Permission数据", resource="Permission数据", action="Permission数据")
        unit_test_db.add(permission)
        unit_test_db.commit()
        
        # 准备测试数据
        entity = Permission(name="查询测试", resource="查询测试", action="查询测试")
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = PermissionRepository.get_by_id(unit_test_db, permission.id)
        
        # 验证结果
        assert result is not None

    def test_get_by_id_not_found(self, unit_test_db: Session):
        """测试get_by_id - 数据不存在"""
        result = PermissionRepository.get_by_id(unit_test_db, "nonexistent_value_12345")
        
        # 验证结果
        assert result is None

    def test_get_by_name_found(self, unit_test_db: Session):
        """测试get_by_name - 查询到数据"""
        # 准备测试数据
        entity = Permission(name="查询测试", resource="查询测试", action="查询测试")
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = PermissionRepository.get_by_name(unit_test_db, entity.name)
        
        # 验证结果
        assert result is not None
        assert result.name == entity.name
    
    def test_get_by_name_not_found(self, unit_test_db: Session):
        """测试get_by_name - 数据不存在"""
        result = PermissionRepository.get_by_name(unit_test_db, "nonexistent_value")
        
        assert result is None

    def test_list_query(self, unit_test_db: Session):
        """测试list - 查询功能"""
        # 准备测试数据
        entity = Permission(name="查询测试", resource="查询测试", action="查询测试")
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        results = PermissionRepository.list(unit_test_db, "", "", 0, 0)
        
        # 验证查询结果
        assert isinstance(results, list)  # 返回列表

    def test_update_single_field(self, unit_test_db: Session):
        """测试update - 单字段更新
        
        符合标准: testing-standards.md 第2.3节 - 只更新一个字段，验证其他字段不变
        """
        # 准备测试数据
        from tests.factories.user_auth_factories import PermissionFactory, UserAuthFactoryManager
        UserAuthFactoryManager.setup_factories(unit_test_db)
        entity = PermissionFactory.create()
        original_resource = entity.resource
        
        # 执行Repository方法（只更新name）
        update_data = {"name": "更新后数据"}
        result = PermissionRepository.update(unit_test_db, entity, update_data)  # TODO: 根据实际方法签名调整
        
        # 验证目标字段已更新
        assert result.name == "更新后数据"
        
        # ✅ 验证其他字段未变化
        assert result.resource == original_resource
        
        # 验证数据库已更新
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(Permission).filter_by(id=entity.id).first()
        assert db_entity.name == "更新后数据"
        assert db_entity.resource == original_resource
    
    def test_update_multiple_fields(self, unit_test_db: Session):
        """测试update - 多字段更新
        
        符合标准: testing-standards.md 第2.3节 - 同时更新多个字段
        """
        from tests.factories.user_auth_factories import PermissionFactory, UserAuthFactoryManager
        UserAuthFactoryManager.setup_factories(unit_test_db)
        entity = PermissionFactory.create()
        
        # 执行Repository方法（同时更新多个字段）
        update_data = {
            "name": "更新后数据1",
            "resource": "更新后数据2"
        }
        result = PermissionRepository.update(unit_test_db, entity, update_data)
        
        # 验证所有字段已更新
        assert result.name == "更新后数据1"
        assert result.resource == "更新后数据2"
        
        # 验证持久化
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(Permission).filter_by(id=entity.id).first()
        assert db_entity.name == "更新后数据1"
        assert db_entity.resource == "更新后数据2"
    
    def test_update_transaction_commit(self, unit_test_db: Session):
        """测试update - 事务提交验证
        
        符合标准: testing-standards.md 第2.5节 - 验证更新真正写入数据库
        """
        from tests.factories.user_auth_factories import PermissionFactory, UserAuthFactoryManager
        UserAuthFactoryManager.setup_factories(unit_test_db)
        entity = PermissionFactory.create()
        
        update_data = {"name": "事务测试数据"}
        result = PermissionRepository.update(unit_test_db, entity, update_data)
        
        # 验证事务已提交
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(Permission).filter_by(id=entity.id).first()
        assert db_entity.name == "事务测试数据"
    
    def test_update_specialized_method(self, unit_test_db: Session):
        """测试update - 专用方法测试（如有）
        
        符合标准: testing-standards.md 第2.3节 - 测试特殊更新方法
        示例: update_status, update_password, activate, deactivate等
        """
        # TODO: 如果有专用更新方法，在这里测试
        # 例如:
        # entity = PermissionFactory.create(status='active')
        # result = PermissionRepository.update_status(unit_test_db, entity.id, 'inactive')
        # assert result.status == 'inactive'
        pass

    def test_delete_success(self, unit_test_db: Session):
        """测试delete - 删除成功"""
        # 准备测试数据
        entity = Permission(name="待删除数据", resource="待删除数据", action="待删除数据")
        unit_test_db.add(entity)
        unit_test_db.commit()
        entity_id = entity.id
        
        # 执行Repository方法（传递对象）
        PermissionRepository.delete(unit_test_db, entity)
        
        # 验证硬删除
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(Permission).filter_by(id=entity_id).first()
        assert db_entity is None  # 记录已物理删除




@pytest.mark.unit
@pytest.mark.repositories
class TestUserRoleRepository:
    """
    UserRoleRepository 数据访问层测试
    
    测试范围:
    - 6 个Repository方法
    - CRUD操作完整性
    - 查询逻辑正确性
    - 事务处理和数据一致性
    
    测试策略: SQLite内存数据库 + Factory Boy
    """
        
    def test_create_minimal_fields(self, unit_test_db: Session):
        """测试create - 最小必填字段创建
        
        符合标准: testing-standards.md 第2.1节 - 只填写必填字段，验证默认值
        数据准备策略: 最小实体构造，不使用Factory Boy
        """
        from tests.factories.user_auth_factories import UserFactory
        from tests.factories.user_auth_factories import UserAuthFactoryManager
        from tests.factories.user_auth_factories import RoleFactory
        # 创建最小实体（只填必填字段）
        UserAuthFactoryManager.setup_factories(unit_test_db)
        user = UserFactory.create()
        role = RoleFactory.create()
        
        entity = UserRole(
            user_id=user.id,
            role_id=role.id
        )
        
        # 执行Repository方法
        result = UserRoleRepository.create(unit_test_db, entity)
        
        # 验证必填字段
        assert result is not None
        
        # 验证默认值（如果模型定义了default）
        # TODO: 根据实际模型补充默认值验证
        
        # 验证数据已持久化（联合主键查询）
        db_entity = unit_test_db.query(UserRole).filter_by(user_id=result.user_id, role_id=result.role_id).first()
        assert db_entity is not None
    
    def test_create_full_fields(self, unit_test_db: Session):
        """测试create - 完整字段创建
        
        符合标准: testing-standards.md 第2.1节 - 填写所有字段，验证保存正确
        """
        # 使用Factory Boy创建完整实体
        from tests.factories.user_auth_factories import UserRoleFactory
        entity = UserRoleFactory.build()  # build不自动保存
        
        # 执行Repository方法
        result = UserRoleRepository.create(unit_test_db, entity)
        
        # 验证所有字段保存正确
        assert result is not None
        # TODO: 验证各个字段值
        
        # 验证持久化
        db_entity = unit_test_db.query(UserRole).filter_by(user_id=result.user_id, role_id=result.role_id).first()
        assert db_entity is not None
    
    def test_create_transaction_commit(self, unit_test_db: Session):
        """测试create - 事务提交验证
        
        符合标准: testing-standards.md 第2.5节 - 验证数据真正写入数据库
        """
        from tests.factories.user_auth_factories import UserRoleFactory
        entity = UserRoleFactory.build()
        
        result = UserRoleRepository.create(unit_test_db, entity)
        
        # 验证事务已提交（expire后重新查询能找到）
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(UserRole).filter_by(user_id=result.user_id, role_id=result.role_id).first()
        assert db_entity is not None

    def test_get_found(self, unit_test_db: Session):
        """测试get - 查询到数据"""
        # 准备测试数据
        entity = user = User(username="依赖查询测试", email="test_依赖查询测试@example.com", password_hash="依赖查询测试", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(user)
        unit_test_db.commit()
        role = Role(name="依赖查询测试", level=1)
        unit_test_db.add(role)
        unit_test_db.commit()
        entity = UserRole(assigned_at=datetime.now(), user_id=user.id, role_id=role.id)
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = UserRoleRepository.get(unit_test_db, entity.user_id, entity.role_id)
        
        # 验证结果
        assert result is not None

    def test_get_not_found(self, unit_test_db: Session):
        """测试get - 数据不存在（复合主键）"""
        result = UserRoleRepository.get(unit_test_db, 999999, 999999)
        
        # 验证结果
        assert result is None

    def test_get_user_roles_found(self, unit_test_db: Session):
        """测试get_user_roles - 查询到数据"""
        # 准备依赖实体和关联数据
        user = User(username="User数据", email="test_user数据@example.com", password_hash="User数据", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(user)
        unit_test_db.commit()
        
        # 准备关联数据（如UserRole关联User和Role）
        entity = user = User(username="依赖关联数据", email="test_依赖关联数据@example.com", password_hash="依赖关联数据", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(user)
        unit_test_db.commit()
        role = Role(name="依赖关联数据", level=1)
        unit_test_db.add(role)
        unit_test_db.commit()
        entity = UserRole(assigned_at=datetime.now(), user_id=user.id, role_id=role.id)
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = UserRoleRepository.get_user_roles(unit_test_db, user.id)
        
        # 验证结果
        assert isinstance(result, list)
        # 注意：复杂join查询可能返回空列表（依赖完整的关联链），
        # 这里只验证方法正确执行并返回list类型即可

    def test_get_user_roles_not_found(self, unit_test_db: Session):
        """测试get_user_roles - 数据不存在"""
        # 准备依赖实体（但不创建关联数据）
        user = User(username="User数据", email="test_user数据@example.com", password_hash="User数据", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(user)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = UserRoleRepository.get_user_roles(unit_test_db, user.id)
        
        # 验证结果
        assert isinstance(result, list)
        assert len(result) == 0

    def test_get_role_users_found(self, unit_test_db: Session):
        """测试get_role_users - 查询到数据"""
        # 准备依赖实体和关联数据
        role = Role(name="Role数据", level=1)
        unit_test_db.add(role)
        unit_test_db.commit()
        
        # 准备关联数据（如UserRole关联User和Role）
        entity = user = User(username="依赖关联数据", email="test_依赖关联数据@example.com", password_hash="依赖关联数据", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(user)
        unit_test_db.commit()
        role = Role(name="依赖关联数据", level=1)
        unit_test_db.add(role)
        unit_test_db.commit()
        entity = UserRole(assigned_at=datetime.now(), user_id=user.id, role_id=role.id)
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = UserRoleRepository.get_role_users(unit_test_db, role.id, 1, 1)
        
        # 验证结果
        assert isinstance(result, list)
        # 注意：复杂join查询可能返回空列表（依赖完整的关联链），
        # 这里只验证方法正确执行并返回list类型即可

    def test_get_role_users_not_found(self, unit_test_db: Session):
        """测试get_role_users - 数据不存在"""
        # 准备依赖实体（但不创建关联数据）
        role = Role(name="Role数据", level=1)
        unit_test_db.add(role)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = UserRoleRepository.get_role_users(unit_test_db, role.id, 1, 1)
        
        # 验证结果
        assert isinstance(result, list)
        assert len(result) == 0

    def test_delete_success(self, unit_test_db: Session):
        """测试delete - 删除成功"""
        # 准备测试数据
        entity = user = User(username="依赖待删除数据", email="test_依赖待删除数据@example.com", password_hash="依赖待删除数据", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(user)
        unit_test_db.commit()
        role = Role(name="依赖待删除数据", level=1)
        unit_test_db.add(role)
        unit_test_db.commit()
        entity = UserRole(assigned_at=datetime.now(), user_id=user.id, role_id=role.id)
        unit_test_db.add(entity)
        unit_test_db.commit()
        user_id_val = entity.user_id
        role_id_val = entity.role_id
        
        # 执行Repository方法（使用主键参数）
        UserRoleRepository.delete(unit_test_db, user_id_val, role_id_val)
        
        # 验证硬删除
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(UserRole).filter_by(user_id=user_id_val, role_id=role_id_val).first()
        assert db_entity is None  # 记录已物理删除

    def test_delete_all_user_roles_success(self, unit_test_db: Session):
        """测试delete_all_user_roles - 删除成功"""
        # 准备测试数据
        entity = user = User(username="依赖待删除数据", email="test_依赖待删除数据@example.com", password_hash="依赖待删除数据", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(user)
        unit_test_db.commit()
        role = Role(name="依赖待删除数据", level=1)
        unit_test_db.add(role)
        unit_test_db.commit()
        entity = UserRole(assigned_at=datetime.now(), user_id=user.id, role_id=role.id)
        unit_test_db.add(entity)
        unit_test_db.commit()
        user_id_val = entity.user_id
        role_id_val = entity.role_id
        
        # 执行Repository方法（使用主键参数）
        UserRoleRepository.delete_all_user_roles(unit_test_db, user_id_val)
        
        # 验证硬删除
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(UserRole).filter_by(user_id=user_id_val, role_id=role_id_val).first()
        assert db_entity is None  # 记录已物理删除




@pytest.mark.unit
@pytest.mark.repositories
class TestRolePermissionRepository:
    """
    RolePermissionRepository 数据访问层测试
    
    测试范围:
    - 6 个Repository方法
    - CRUD操作完整性
    - 查询逻辑正确性
    - 事务处理和数据一致性
    
    测试策略: SQLite内存数据库 + Factory Boy
    """
        
    def test_create_minimal_fields(self, unit_test_db: Session):
        """测试create - 最小必填字段创建
        
        符合标准: testing-standards.md 第2.1节 - 只填写必填字段，验证默认值
        数据准备策略: 最小实体构造，不使用Factory Boy
        """
        from tests.factories.user_auth_factories import RoleFactory
        from tests.factories.user_auth_factories import UserAuthFactoryManager
        from tests.factories.user_auth_factories import PermissionFactory
        # 创建最小实体（只填必填字段）
        UserAuthFactoryManager.setup_factories(unit_test_db)
        role = RoleFactory.create()
        permission = PermissionFactory.create()
        
        entity = RolePermission(
            role_id=role.id,
            permission_id=permission.id
        )
        
        # 执行Repository方法
        result = RolePermissionRepository.create(unit_test_db, entity)
        
        # 验证必填字段
        assert result is not None
        
        # 验证默认值（如果模型定义了default）
        # TODO: 根据实际模型补充默认值验证
        
        # 验证数据已持久化（联合主键查询）
        db_entity = unit_test_db.query(RolePermission).filter_by(role_id=result.role_id, permission_id=result.permission_id).first()
        assert db_entity is not None
    
    def test_create_full_fields(self, unit_test_db: Session):
        """测试create - 完整字段创建
        
        符合标准: testing-standards.md 第2.1节 - 填写所有字段，验证保存正确
        """
        # 使用Factory Boy创建完整实体
        from tests.factories.user_auth_factories import RolePermissionFactory
        entity = RolePermissionFactory.build()  # build不自动保存
        
        # 执行Repository方法
        result = RolePermissionRepository.create(unit_test_db, entity)
        
        # 验证所有字段保存正确
        assert result is not None
        # TODO: 验证各个字段值
        
        # 验证持久化
        db_entity = unit_test_db.query(RolePermission).filter_by(role_id=result.role_id, permission_id=result.permission_id).first()
        assert db_entity is not None
    
    def test_create_transaction_commit(self, unit_test_db: Session):
        """测试create - 事务提交验证
        
        符合标准: testing-standards.md 第2.5节 - 验证数据真正写入数据库
        """
        from tests.factories.user_auth_factories import RolePermissionFactory
        entity = RolePermissionFactory.build()
        
        result = RolePermissionRepository.create(unit_test_db, entity)
        
        # 验证事务已提交（expire后重新查询能找到）
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(RolePermission).filter_by(role_id=result.role_id, permission_id=result.permission_id).first()
        assert db_entity is not None

    def test_get_found(self, unit_test_db: Session):
        """测试get - 查询到数据"""
        # 准备测试数据
        entity = role = Role(name="依赖查询测试", level=1)
        unit_test_db.add(role)
        unit_test_db.commit()
        permission = Permission(name="依赖查询测试", resource="依赖查询测试", action="依赖查询测试")
        unit_test_db.add(permission)
        unit_test_db.commit()
        entity = RolePermission(granted_at=datetime.now(), role_id=role.id, permission_id=permission.id)
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = RolePermissionRepository.get(unit_test_db, entity.role_id, entity.permission_id)
        
        # 验证结果
        assert result is not None

    def test_get_not_found(self, unit_test_db: Session):
        """测试get - 数据不存在（复合主键）"""
        result = RolePermissionRepository.get(unit_test_db, 999999, 999999)
        
        # 验证结果
        assert result is None

    def test_get_role_permissions_found(self, unit_test_db: Session):
        """测试get_role_permissions - 查询到数据"""
        # 准备依赖实体和关联数据
        role = Role(name="Role数据", level=1)
        unit_test_db.add(role)
        unit_test_db.commit()
        
        # 准备关联数据（如UserRole关联User和Role）
        entity = role = Role(name="依赖关联数据", level=1)
        unit_test_db.add(role)
        unit_test_db.commit()
        permission = Permission(name="依赖关联数据", resource="依赖关联数据", action="依赖关联数据")
        unit_test_db.add(permission)
        unit_test_db.commit()
        entity = RolePermission(granted_at=datetime.now(), role_id=role.id, permission_id=permission.id)
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = RolePermissionRepository.get_role_permissions(unit_test_db, role.id)
        
        # 验证结果
        assert isinstance(result, list)
        # 注意：复杂join查询可能返回空列表（依赖完整的关联链），
        # 这里只验证方法正确执行并返回list类型即可

    def test_get_role_permissions_not_found(self, unit_test_db: Session):
        """测试get_role_permissions - 数据不存在"""
        # 准备依赖实体（但不创建关联数据）
        role = Role(name="Role数据", level=1)
        unit_test_db.add(role)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = RolePermissionRepository.get_role_permissions(unit_test_db, role.id)
        
        # 验证结果
        assert isinstance(result, list)
        assert len(result) == 0

    def test_get_user_permissions_found(self, unit_test_db: Session):
        """测试get_user_permissions - 查询到数据"""
        # 准备依赖实体和关联数据
        user = User(username="User数据", email="test_user数据@example.com", password_hash="User数据", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(user)
        unit_test_db.commit()
        
        # 准备关联数据（如UserRole关联User和Role）
        entity = role = Role(name="依赖关联数据", level=1)
        unit_test_db.add(role)
        unit_test_db.commit()
        permission = Permission(name="依赖关联数据", resource="依赖关联数据", action="依赖关联数据")
        unit_test_db.add(permission)
        unit_test_db.commit()
        entity = RolePermission(granted_at=datetime.now(), role_id=role.id, permission_id=permission.id)
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = RolePermissionRepository.get_user_permissions(unit_test_db, user.id)
        
        # 验证结果
        assert isinstance(result, list)
        # 注意：复杂join查询可能返回空列表（依赖完整的关联链），
        # 这里只验证方法正确执行并返回list类型即可

    def test_get_user_permissions_not_found(self, unit_test_db: Session):
        """测试get_user_permissions - 数据不存在"""
        # 准备依赖实体（但不创建关联数据）
        user = User(username="User数据", email="test_user数据@example.com", password_hash="User数据", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(user)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = RolePermissionRepository.get_user_permissions(unit_test_db, user.id)
        
        # 验证结果
        assert isinstance(result, list)
        assert len(result) == 0

    def test_delete_success(self, unit_test_db: Session):
        """测试delete - 删除成功"""
        # 准备测试数据
        entity = role = Role(name="依赖待删除数据", level=1)
        unit_test_db.add(role)
        unit_test_db.commit()
        permission = Permission(name="依赖待删除数据", resource="依赖待删除数据", action="依赖待删除数据")
        unit_test_db.add(permission)
        unit_test_db.commit()
        entity = RolePermission(granted_at=datetime.now(), role_id=role.id, permission_id=permission.id)
        unit_test_db.add(entity)
        unit_test_db.commit()
        role_id_val = entity.role_id
        permission_id_val = entity.permission_id
        
        # 执行Repository方法（使用主键参数）
        RolePermissionRepository.delete(unit_test_db, role_id_val, permission_id_val)
        
        # 验证硬删除
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(RolePermission).filter_by(role_id=role_id_val, permission_id=permission_id_val).first()
        assert db_entity is None  # 记录已物理删除

    def test_delete_all_role_permissions_success(self, unit_test_db: Session):
        """测试delete_all_role_permissions - 删除成功"""
        # 准备测试数据
        entity = role = Role(name="依赖待删除数据", level=1)
        unit_test_db.add(role)
        unit_test_db.commit()
        permission = Permission(name="依赖待删除数据", resource="依赖待删除数据", action="依赖待删除数据")
        unit_test_db.add(permission)
        unit_test_db.commit()
        entity = RolePermission(granted_at=datetime.now(), role_id=role.id, permission_id=permission.id)
        unit_test_db.add(entity)
        unit_test_db.commit()
        role_id_val = entity.role_id
        permission_id_val = entity.permission_id
        
        # 执行Repository方法（使用主键参数）
        RolePermissionRepository.delete_all_role_permissions(unit_test_db, role_id_val)
        
        # 验证硬删除
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(RolePermission).filter_by(role_id=role_id_val, permission_id=permission_id_val).first()
        assert db_entity is None  # 记录已物理删除




@pytest.mark.unit
@pytest.mark.repositories
class TestSessionRepository:
    """
    SessionRepository 数据访问层测试
    
    测试范围:
    - 9 个Repository方法
    - CRUD操作完整性
    - 查询逻辑正确性
    - 事务处理和数据一致性
    
    测试策略: SQLite内存数据库 + Factory Boy
    """
        
    def test_create_minimal_fields(self, unit_test_db: Session):
        """测试create - 最小必填字段创建
        
        符合标准: testing-standards.md 第2.1节 - 只填写必填字段，验证默认值
        数据准备策略: 最小实体构造，不使用Factory Boy
        """
        from tests.factories.user_auth_factories import UserFactory
        from tests.factories.user_auth_factories import UserAuthFactoryManager
        # 创建最小实体（只填必填字段）
        UserAuthFactoryManager.setup_factories(unit_test_db)
        user = UserFactory.create()
        
        entity = Session(
            token_hash="token_hash",
            expires_at=datetime.now(),
            is_active=False,
            user_id=user.id
        )
        
        # 执行Repository方法
        result = SessionRepository.create(unit_test_db, entity)
        
        # 验证必填字段
        assert result is not None
        assert result.id is not None  # 验证ID已生成
        
        # 验证默认值（Column(default=...)定义的值）
        # 示例: assert result.is_active == True
        # 示例: assert result.status == "active"
        # TODO: 根据实际模型补充默认值验证
        
        # 验证数据已持久化
        db_entity = unit_test_db.query(Session).filter_by(id=result.id).first()
        assert db_entity is not None
    
    def test_create_full_fields(self, unit_test_db: Session):
        """测试create - 完整字段创建
        
        符合标准: testing-standards.md 第2.1节 - 填写所有字段，验证保存正确
        数据准备策略: 使用Factory Boy
        """
        # 使用Factory Boy创建完整实体
        from tests.factories.user_auth_factories import SessionFactory
        entity = SessionFactory.build()  # build不自动保存到数据库
        
        # 执行Repository方法
        result = SessionRepository.create(unit_test_db, entity)
        
        # 验证所有字段保存正确
        assert result is not None
        assert result.id is not None
        # TODO: 验证其他字段值正确保存
        
        # 验证持久化
        db_entity = unit_test_db.query(Session).filter_by(id=result.id).first()
        assert db_entity is not None
    
    def test_create_transaction_commit(self, unit_test_db: Session):
        """测试create - 事务提交验证
        
        符合标准: testing-standards.md 第2.5节 - 验证数据真正写入数据库
        """
        from tests.factories.user_auth_factories import SessionFactory
        entity = SessionFactory.build()
        
        result = SessionRepository.create(unit_test_db, entity)
        
        # 验证事务已提交（expire后重新查询能找到）
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(Session).filter_by(id=result.id).first()
        assert db_entity is not None

    def test_get_by_id_found(self, unit_test_db: Session):
        """测试get_by_id - 查询到数据"""
        # 准备依赖实体
        session = user = User(username="依赖Session数据", email="test_依赖session数据@example.com", password_hash="依赖Session数据", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(user)
        unit_test_db.commit()
        entity = Session(token_hash="Session数据", expires_at=datetime.now(), last_accessed_at=datetime.now(), is_active=True, user_id=user.id)
        unit_test_db.add(session)
        unit_test_db.commit()
        
        # 准备测试数据
        entity = user = User(username="依赖查询测试", email="test_依赖查询测试@example.com", password_hash="依赖查询测试", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(user)
        unit_test_db.commit()
        entity = Session(token_hash="查询测试", expires_at=datetime.now(), last_accessed_at=datetime.now(), is_active=True, user_id=user.id)
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = SessionRepository.get_by_id(unit_test_db, session.id)
        
        # 验证结果
        assert result is not None

    def test_get_by_id_not_found(self, unit_test_db: Session):
        """测试get_by_id - 数据不存在"""
        result = SessionRepository.get_by_id(unit_test_db, "nonexistent_value_12345")
        
        # 验证结果
        assert result is None

    def test_get_by_token_hash_found(self, unit_test_db: Session):
        """测试get_by_token_hash - 查询到数据"""
        # 准备测试数据
        entity = user = User(username="依赖查询测试", email="test_依赖查询测试@example.com", password_hash="依赖查询测试", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(user)
        unit_test_db.commit()
        entity = Session(token_hash="查询测试", expires_at=datetime.now(), last_accessed_at=datetime.now(), is_active=True, user_id=user.id)
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = SessionRepository.get_by_token_hash(unit_test_db, entity.token_hash)
        
        # 验证结果
        assert result is not None
        assert result.token_hash == entity.token_hash
    
    def test_get_by_token_hash_not_found(self, unit_test_db: Session):
        """测试get_by_token_hash - 数据不存在"""
        result = SessionRepository.get_by_token_hash(unit_test_db, "nonexistent_value")
        
        assert result is None

    def test_get_user_active_sessions_found(self, unit_test_db: Session):
        """测试get_user_active_sessions - 查询到数据"""
        # 准备依赖实体和关联数据
        user = User(username="User数据", email="test_user数据@example.com", password_hash="User数据", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(user)
        unit_test_db.commit()
        
        # 准备关联数据（如UserRole关联User和Role）
        entity = user = User(username="依赖关联数据", email="test_依赖关联数据@example.com", password_hash="依赖关联数据", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(user)
        unit_test_db.commit()
        entity = Session(token_hash="关联数据", expires_at=datetime.now(), last_accessed_at=datetime.now(), is_active=True, user_id=user.id)
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = SessionRepository.get_user_active_sessions(unit_test_db, user.id)
        
        # 验证结果
        assert isinstance(result, list)
        # 注意：复杂join查询可能返回空列表（依赖完整的关联链），
        # 这里只验证方法正确执行并返回list类型即可

    def test_get_user_active_sessions_not_found(self, unit_test_db: Session):
        """测试get_user_active_sessions - 数据不存在"""
        # 准备依赖实体（但不创建关联数据）
        user = User(username="User数据", email="test_user数据@example.com", password_hash="User数据", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(user)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = SessionRepository.get_user_active_sessions(unit_test_db, user.id)
        
        # 验证结果
        assert isinstance(result, list)
        assert len(result) == 0

    # TODO: 测试专用更新方法 update_last_accessed
    # 这是一个专用更新方法，只修改特定字段，需要根据业务逻辑手动编写测试
    # 方法签名: [('session', 'UserSession')]
    # 返回类型: UserSession
    

    def test_deactivate_query(self, unit_test_db: Session):
        """测试deactivate - 查询功能"""
        # 准备测试数据
        entity = user = User(username="依赖查询测试", email="test_依赖查询测试@example.com", password_hash="依赖查询测试", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(user)
        unit_test_db.commit()
        entity = Session(token_hash="查询测试", expires_at=datetime.now(), last_accessed_at=datetime.now(), is_active=True, user_id=user.id)
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = SessionRepository.deactivate(unit_test_db, entity)
        
        # 验证查询结果
        assert result is not None

    def test_deactivate_user_sessions_query(self, unit_test_db: Session):
        """测试deactivate_user_sessions - 查询功能"""
        # 准备测试数据
        entity = user = User(username="依赖查询测试", email="test_依赖查询测试@example.com", password_hash="依赖查询测试", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(user)
        unit_test_db.commit()
        entity = Session(token_hash="查询测试", expires_at=datetime.now(), last_accessed_at=datetime.now(), is_active=True, user_id=user.id)
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行Repository方法
        result = SessionRepository.deactivate_user_sessions(unit_test_db, entity.id)
        
        # 验证查询结果
        # 方法返回None，验证执行成功即可

    def test_delete_expired_sessions_physical_delete(self, unit_test_db: Session):
        """测试delete_expired_sessions - 批量删除功能
        
        符合标准: testing-standards.md 第2.4节 - 验证批量删除功能
        """
        # 创建测试数据（使用手动创建避免Factory SubFactory session问题）
        entity = user = User(username="依赖批量删除测试", email="test_依赖批量删除测试@example.com", password_hash="依赖批量删除测试", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(user)
        unit_test_db.commit()
        entity = Session(token_hash="批量删除测试", expires_at=datetime.now(), last_accessed_at=datetime.now(), is_active=True, user_id=user.id)
        unit_test_db.add(entity)
        unit_test_db.commit()
        
        # 执行批量删除（方法根据内部条件删除数据）
        result = SessionRepository.delete_expired_sessions(unit_test_db)
        
        # ✅ 验证返回结果（通常返回删除的数量）
        if result is not None:
            assert isinstance(result, int)
            assert result >= 0
        
        # 验证数据库操作成功
        total_count = unit_test_db.query(Session).count()
        assert total_count >= 0
    
    def test_delete_expired_sessions_cascade_delete(self, unit_test_db: Session):
        """测试delete_expired_sessions - 级联删除（如有关联数据）
        
        符合标准: testing-standards.md 第2.4节 - 验证关联数据的物理删除
        """
        # TODO: 如果该模型有关联数据，验证级联删除行为
        # 根据外键定义的ondelete行为:
        # - CASCADE: 子记录应被删除
        # - SET NULL: 子记录外键应为NULL
        # - RESTRICT: 应抛出异常
        
        # 示例:
        # parent = ParentFactory.create()
        # child = ChildFactory.create(parent_id=parent.id)
        # SessionRepository.delete_expired_sessions(unit_test_db, parent.id)
        # 
        # # CASCADE情况
        # db_child = unit_test_db.query(Child).filter_by(id=child.id).first()
        # assert db_child is None
        pass
    
    def test_delete_expired_sessions_batch_delete(self, unit_test_db: Session):
        """测试delete_expired_sessions - 批量物理删除（如有批量方法）
        
        符合标准: testing-standards.md 第2.4节 - 验证批量删除
        """
        # TODO: 如果有批量删除方法(delete_many, bulk_delete)，在这里测试
        # 示例:
        # entities = SessionFactory.create_batch(3)
        # ids = [e.id for e in entities]
        # SessionRepository.delete_many(unit_test_db, ids)
        # 
        # for entity_id in ids:
        #     db_entity = unit_test_db.query(Session).filter_by(id=entity_id).first()
        #     assert db_entity is None
        pass

    def test_delete_success(self, unit_test_db: Session):
        """测试delete - 删除成功"""
        # 准备测试数据
        entity = user = User(username="依赖待删除数据", email="test_依赖待删除数据@example.com", password_hash="依赖待删除数据", is_active=True, status="active", email_verified=True, phone_verified=True, two_factor_enabled=True, failed_login_attempts=1, role="user")
        unit_test_db.add(user)
        unit_test_db.commit()
        entity = Session(token_hash="待删除数据", expires_at=datetime.now(), last_accessed_at=datetime.now(), is_active=True, user_id=user.id)
        unit_test_db.add(entity)
        unit_test_db.commit()
        entity_id = entity.id
        
        # 执行Repository方法（传递对象）
        SessionRepository.delete(unit_test_db, entity)
        
        # 验证硬删除
        unit_test_db.expire_all()
        db_entity = unit_test_db.query(Session).filter_by(id=entity_id).first()
        assert db_entity is None  # 记录已物理删除

