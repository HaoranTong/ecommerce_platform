# Auto-generated test file
# Generated at: 20251021_234902
# Target path: tests/unit/test_services/test_shopping_cart_services.py
# Do NOT edit this file manually - regenerate from source models

"""
Shopping_Cart service tests generated by ServiceTestGenerator.

Generated: 2025-10-21 15:49:02
Strategy: repository mocks only.
"""

import inspect
from types import SimpleNamespace
from decimal import Decimal
from datetime import datetime, timedelta
import pytest
from pytest_mock import MockerFixture

from app.modules.shopping_cart.models import Cart, CartItem
from app.modules.shopping_cart.repository import CartItemRepository, CartRepository

try:
    from app.modules.shopping_cart.service import CartService
    SERVICE_AVAILABLE = True
except ImportError:
    SERVICE_AVAILABLE = False

def _pick_dependency(context: dict, keyword: str):
    for name, value in context.items():
        if name == "service":
            continue
        if keyword.lower() in name.lower():
            return value
    return None


def _make_namespace(**overrides):
    defaults = {
        "id": 1,
        "status": "pending",
        "amount": Decimal("99.99"),
        "order_id": 1,
        "user_id": 1,
        "payment_reference": "PAY-AUTO-001",
        "description": "auto generated",
    }
    defaults.update(overrides)
    return SimpleNamespace(**defaults)


def _make_session_mock(mocker: MockerFixture):
    session = mocker.MagicMock(name="CartService_session")
    session.begin.return_value.__enter__.return_value = session
    session.begin.return_value.__exit__.return_value = False
    session.begin_nested.return_value.__enter__.return_value = session
    session.begin_nested.return_value.__exit__.return_value = False
    query = session.query.return_value
    query.filter.return_value = query
    query.filter_by.return_value = query
    query.one_or_none.return_value = _make_namespace()
    query.all.return_value = []
    query.count.return_value = 0
    session.refresh.side_effect = lambda entity: setattr(entity, "id", getattr(entity, "id", 1))
    session.commit.return_value = None
    session.rollback.return_value = None
    return session


def _configure_repository_session(repository_mock, mocker: MockerFixture):
    session = _make_session_mock(mocker)
    repository_mock.session = session
    return session


async def _run_in_thread_passthrough(func, *args, **kwargs):
    result = func(*args, **kwargs)
    if inspect.isawaitable(result):
        return await result
    return result


@pytest.fixture
def service_context(mocker: MockerFixture):
    if not SERVICE_AVAILABLE:
        pytest.skip("service import failed")

    service_cls = CartService
    signature = inspect.signature(service_cls)
    init_kwargs = {}
    context = {}

    for name, param in signature.parameters.items():
        if name == "self":
            continue
        dependency = mocker.MagicMock(name=name)
        context[name] = dependency
        init_kwargs[name] = dependency
        if "repository" in name.lower():
            _configure_repository_session(dependency, mocker)
            # configure common repository methods to return sensible defaults
            try:
                dependency.get_or_create.return_value = _make_namespace(id=1)
            except Exception:
                pass
            try:
                dependency.find_by_user_id.return_value = _make_namespace(id=1, user_id=1)
            except Exception:
                pass
            try:
                dependency.find_by_cart_and_sku.return_value = None
            except Exception:
                pass
            try:
                dependency.find_by_cart_id.return_value = []
            except Exception:
                pass
            try:
                dependency.count_by_cart_id.return_value = 0
            except Exception:
                pass

    service = service_cls(**init_kwargs)
    # replace internal repository instances on the service with MagicMocks so tests can stub their methods
    try:
        cart_repo_mock = mocker.MagicMock(name="cart_repo")
        cart_stub = _make_namespace(id=1, user_id=1, created_at=datetime.utcnow(), updated_at=datetime.utcnow())
        cart_repo_mock.get_or_create.return_value = cart_stub
        cart_repo_mock.find_by_user_id.return_value = cart_stub
        cart_repo_mock.find_by_id.return_value = cart_stub
        cart_repo_mock.create.return_value = cart_stub
        cart_repo_mock.update_timestamp.return_value = None
        cart_repo_mock.delete.return_value = None

        cart_item_repo_mock = mocker.MagicMock(name="cart_item_repo")
        cart_item_stub = _make_namespace(
            id=1,
            cart_id=cart_stub.id,
            sku_id=1,
            unit_price=Decimal("99.99"),
            quantity=1,
            created_at=datetime.utcnow(),
        )
        cart_item_repo_mock.find_by_cart_and_sku.return_value = None
        cart_item_repo_mock.find_by_cart_id.return_value = [cart_item_stub]
        cart_item_repo_mock.find_by_id_and_user.return_value = cart_item_stub
        cart_item_repo_mock.update.return_value = None
        cart_item_repo_mock.create.return_value = cart_item_stub
        cart_item_repo_mock.delete.return_value = None
        cart_item_repo_mock.delete_by_ids.return_value = 1
        cart_item_repo_mock.delete_by_cart_id.return_value = None
        cart_item_repo_mock.count_by_cart_id.return_value = 0

        setattr(service, "cart_repo", cart_repo_mock)
        setattr(service, "cart_item_repo", cart_item_repo_mock)
    except Exception:
        pass
    context["service"] = service
    return context

@pytest.mark.unit
@pytest.mark.services
class TestCartService:
    """Service layer tests relying on mocked repositories."""

    def test_service_initialization(self, service_context: dict) -> None:
        if not SERVICE_AVAILABLE:
            pytest.skip("service import failed")
        assert service_context["service"] is not None

    @pytest.mark.asyncio
    async def test_add_item_success(self, mocker: MockerFixture, service_context: dict) -> None:
        """Exercise add_item success path."""
        if not SERVICE_AVAILABLE:
            pytest.skip("service import failed")
        context = service_context
        service = context["service"]
        utcnow_call_mock = mocker.patch("app.modules.shopping_cart.service.datetime")
        utcnow_call_mock.utcnow.return_value = datetime.utcnow()
        CartItem_call_mock = mocker.patch("app.modules.shopping_cart.service.CartItem")
        CartItem_call_mock.return_value = None
        user_id = 1
        request = _make_namespace(quantity=1, sku_id=1)
        result = await service.add_item(user_id=user_id, request=request)
        assert result is not None

    @pytest.mark.asyncio
    async def test_get_cart_success(self, mocker: MockerFixture, service_context: dict) -> None:
        """Exercise get_cart success path."""
        if not SERVICE_AVAILABLE:
            pytest.skip("service import failed")
        context = service_context
        service = context["service"]
        utcnow_call_mock = mocker.patch("app.modules.shopping_cart.service.datetime")
        utcnow_call_mock.utcnow.return_value = datetime.utcnow()
        CartItemResponse_call_mock = mocker.patch("app.modules.shopping_cart.service.CartItemResponse")
        CartItemResponse_call_mock.return_value = _make_namespace()
        CartResponse_call_mock = mocker.patch("app.modules.shopping_cart.service.CartResponse")
        CartResponse_call_mock.return_value = _make_namespace()
        user_id = 1
        result = await service.get_cart(user_id=user_id)
        assert result is not None

    @pytest.mark.asyncio
    async def test_update_quantity_success(self, mocker: MockerFixture, service_context: dict) -> None:
        """Exercise update_quantity success path."""
        if not SERVICE_AVAILABLE:
            pytest.skip("service import failed")
        context = service_context
        service = context["service"]
        user_id = 1
        item_id = 1
        quantity = 1
        result = await service.update_quantity(user_id=user_id, item_id=item_id, quantity=quantity)
        assert result is not None

    @pytest.mark.asyncio
    async def test_delete_item_success(self, mocker: MockerFixture, service_context: dict) -> None:
        """Exercise delete_item success path."""
        if not SERVICE_AVAILABLE:
            pytest.skip("service import failed")
        context = service_context
        service = context["service"]
        user_id = 1
        item_id = 1
        result = await service.delete_item(user_id=user_id, item_id=item_id)
        assert result is not None

    @pytest.mark.asyncio
    async def test_batch_delete_items_success(self, mocker: MockerFixture, service_context: dict) -> None:
        """Exercise batch_delete_items success path."""
        if not SERVICE_AVAILABLE:
            pytest.skip("service import failed")
        context = service_context
        service = context["service"]
        user_id = 1
        item_ids = 1
        result = await service.batch_delete_items(user_id=user_id, item_ids=item_ids)
        assert result is not None

    @pytest.mark.asyncio
    async def test_clear_cart_success(self, mocker: MockerFixture, service_context: dict) -> None:
        """Exercise clear_cart success path."""
        if not SERVICE_AVAILABLE:
            pytest.skip("service import failed")
        context = service_context
        service = context["service"]
        user_id = 1
        result = await service.clear_cart(user_id=user_id)
        assert result is not None

