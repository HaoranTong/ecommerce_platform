# Auto-generated test file
# Generated at: 20251022_135511
# Target path: tests/unit/test_services/test_payment_service_services.py
# Do NOT edit this file manually - regenerate from source models

"""
Payment_Service service tests generated by ServiceTestGenerator.

Generated: 2025-10-22 05:55:11
Strategy: repository mocks only.
"""

import inspect
from types import SimpleNamespace
from decimal import Decimal
from datetime import datetime, timedelta
import pytest
from pytest_mock import MockerFixture

from app.modules.payment_service.models import Payment
from app.modules.payment_service.repository import PaymentRepository

try:
    from app.modules.payment_service.service import PaymentService
    SERVICE_AVAILABLE = True
except ImportError:
    SERVICE_AVAILABLE = False

def _pick_dependency(context: dict, keyword: str):
    for name, value in context.items():
        if name == "service":
            continue
        if keyword.lower() in name.lower():
            return value
    return None


def _make_namespace(**overrides):
    defaults = {
        "id": 1,
        "status": "pending",
        "amount": Decimal("99.99"),
        "order_id": 1,
        "user_id": 1,
        "description": "auto generated",
        "created_at": datetime.utcnow(),
        "updated_at": datetime.utcnow(),
        "payment_reference": "PAY-AUTO-001"
    }
    defaults.update(overrides)
    namespace = SimpleNamespace(**defaults)

    return namespace


def _make_session_mock(mocker: MockerFixture):
    session = mocker.MagicMock(name="PaymentService_session")
    session.begin.return_value.__enter__.return_value = session
    session.begin.return_value.__exit__.return_value = False
    session.begin_nested.return_value.__enter__.return_value = session
    session.begin_nested.return_value.__exit__.return_value = False
    query = session.query.return_value
    query.filter.return_value = query
    query.filter_by.return_value = query
    query.one_or_none.return_value = _make_namespace()
    query.all.return_value = []
    query.count.return_value = 0
    session.refresh.side_effect = lambda entity: setattr(entity, "id", getattr(entity, "id", 1))
    session.commit.return_value = None
    session.rollback.return_value = None
    return session


def _configure_repository_session(repository_mock, mocker: MockerFixture):
    session = _make_session_mock(mocker)
    repository_mock.session = session
    return session


async def _run_in_thread_passthrough(func, *args, **kwargs):
    result = func(*args, **kwargs)
    if inspect.isawaitable(result):
        return await result
    return result


@pytest.fixture
def service_context(mocker: MockerFixture):
    if not SERVICE_AVAILABLE:
        pytest.skip("service import failed")

    service_cls = PaymentService
    signature = inspect.signature(service_cls)
    init_kwargs = {}
    context = {}

    for name, param in signature.parameters.items():
        if name == "self":
            continue
        dependency = mocker.MagicMock(name=name)
        context[name] = dependency
        init_kwargs[name] = dependency
        if "repository" in name.lower():
            _configure_repository_session(dependency, mocker)

    service = service_cls(**init_kwargs)
    try:
        db_execute_result = mocker.MagicMock(name="db_execute_result")
        db_execute_result.scalars.return_value = mocker.MagicMock(name="db_scalars_result")
        db_execute_result.scalars.return_value.all.return_value = []
        service.db.execute.return_value = db_execute_result
    except Exception:
        pass
    try:
        if hasattr(service, "repository"):
            repository_mock = mocker.MagicMock(name="repository")
            _configure_repository_session(repository_mock, mocker)

            setattr(service, "repository", repository_mock)
            context["repository"] = repository_mock
    except Exception:
        pass
    context["service"] = service
    return context

@pytest.mark.unit
@pytest.mark.services
class TestPaymentService:
    """Service layer tests relying on mocked repositories."""

    def test_service_initialization(self, service_context: dict) -> None:
        if not SERVICE_AVAILABLE:
            pytest.skip("service import failed")
        assert service_context["service"] is not None

    def test_create_payment_success(self, mocker: MockerFixture, service_context: dict) -> None:
        """Exercise create_payment success path."""
        if not SERVICE_AVAILABLE:
            pytest.skip("service import failed")
        context = service_context
        service = context["service"]
        service_cls = service.__class__
        create_payment_audit_log_call_mock = mocker.patch("app.modules.payment_service.service.create_payment_audit_log")
        create_payment_audit_log_call_mock.return_value = _make_namespace(payment_no='PAYMENT-001', amount=Decimal('99.99'), currency='CNY', payment_method='wechat')
        utcnow_call_mock = mocker.patch("app.modules.payment_service.service.datetime")
        utcnow_call_mock.utcnow.return_value = datetime.utcnow()
        Payment_call_mock = mocker.patch("app.modules.payment_service.service.Payment")
        Payment_call_mock.return_value = _make_namespace(id=1, payment_no='PAYMENT-001', amount=Decimal('99.99'), currency='CNY', payment_method='wechat')
        PaymentDomainEvent_call_mock = mocker.patch("app.modules.payment_service.service.PaymentDomainEvent")
        PaymentDomainEvent_call_mock.return_value = _make_namespace(payment_no='PAYMENT-001', amount=Decimal('99.99'), currency='CNY', payment_method='wechat')
        repository = context.get("repository") or getattr(service, "repository", None) or _pick_dependency(context, "repository")
        if repository:
            repository.find_active_for_order.return_value = None
            repository.create_payment.return_value = _make_namespace(id=1)
            repository.enqueue_event.return_value = None
        _transaction_scope__transaction_scope_patch_target = service_cls if hasattr(service_cls, "_transaction_scope") else service
        _transaction_scope__transaction_scope_mock = mocker.patch.object(_transaction_scope__transaction_scope_patch_target, "_transaction_scope")
        json_dumps_mock = mocker.patch("json.dumps")
        json_dumps_mock.return_value = None
        order = _make_namespace(id=1, total_amount=Decimal("99.99"))
        payload = _make_namespace(amount=Decimal("99.99"), currency="value", description="value", model_dump="value", payment_method="value")
        payload.model_dump = lambda **kwargs: dict(payload.__dict__)
        current_user = _make_namespace(id=1)
        result = service.create_payment(order=order, payload=payload, current_user=current_user)
        assert result is not None
        if repository:
            repository.create_payment.assert_called()
            repository.enqueue_event.assert_called()
            repository.find_active_for_order.assert_called()
        json_dumps_mock.assert_called()
        _transaction_scope__transaction_scope_mock.assert_called()

    def test_get_payment_success(self, mocker: MockerFixture, service_context: dict) -> None:
        """Exercise get_payment success path."""
        if not SERVICE_AVAILABLE:
            pytest.skip("service import failed")
        context = service_context
        service = context["service"]
        service_cls = service.__class__
        repository = context.get("repository") or getattr(service, "repository", None) or _pick_dependency(context, "repository")
        if repository:
            payment_stub = _make_namespace()
            repository.get_by_id.return_value = payment_stub
        _can_view_payment__can_view_payment_patch_target = service_cls if hasattr(service_cls, "_can_view_payment") else service
        _can_view_payment__can_view_payment_mock = mocker.patch.object(_can_view_payment__can_view_payment_patch_target, "_can_view_payment")
        _can_view_payment__can_view_payment_mock.return_value = True
        payment_id = None
        current_user = None
        result = service.get_payment(payment_id=payment_id, current_user=current_user)
        assert result is not None
        if repository:
            repository.get_by_id.assert_called()
        _can_view_payment__can_view_payment_mock.assert_called()

    def test_list_payments_for_user_success(self, mocker: MockerFixture, service_context: dict) -> None:
        """Exercise list_payments_for_user success path."""
        if not SERVICE_AVAILABLE:
            pytest.skip("service import failed")
        context = service_context
        service = context["service"]
        service_cls = service.__class__
        repository = context.get("repository") or getattr(service, "repository", None) or _pick_dependency(context, "repository")
        if repository:
            repository.list_user_payments.return_value = []
        current_user = _make_namespace(id=1)
        order_id = None
        status_filter = None
        limit = None
        offset = None
        result = service.list_payments_for_user(current_user=current_user, order_id=order_id, status_filter=status_filter, limit=limit, offset=offset)
        assert result is not None
        if repository:
            repository.list_user_payments.assert_called()

    def test_list_payments_for_admin_success(self, mocker: MockerFixture, service_context: dict) -> None:
        """Exercise list_payments_for_admin success path."""
        if not SERVICE_AVAILABLE:
            pytest.skip("service import failed")
        context = service_context
        service = context["service"]
        service_cls = service.__class__
        repository = context.get("repository") or getattr(service, "repository", None) or _pick_dependency(context, "repository")
        if repository:
            repository.list_admin_payments.return_value = []
        order_id = None
        user_id = None
        status_filter = None
        payment_method = None
        limit = None
        offset = None
        result = service.list_payments_for_admin(order_id=order_id, user_id=user_id, status_filter=status_filter, payment_method=payment_method, limit=limit, offset=offset)
        assert result is not None
        if repository:
            repository.list_admin_payments.assert_called()

    def test_process_wechat_callback_success(self, mocker: MockerFixture, service_context: dict) -> None:
        """Exercise process_wechat_callback success path."""
        if not SERVICE_AVAILABLE:
            pytest.skip("service import failed")
        context = service_context
        service = context["service"]
        service_cls = service.__class__
        create_payment_audit_log_call_mock = mocker.patch("app.modules.payment_service.service.create_payment_audit_log")
        create_payment_audit_log_call_mock.return_value = _make_namespace(payment_no='PAYMENT-001', amount=Decimal('99.99'), currency='CNY', payment_method='wechat')
        utcnow_call_mock = mocker.patch("app.modules.payment_service.service.datetime")
        utcnow_call_mock.utcnow.return_value = datetime.utcnow()
        PaymentDomainEvent_call_mock = mocker.patch("app.modules.payment_service.service.PaymentDomainEvent")
        PaymentDomainEvent_call_mock.return_value = _make_namespace(payment_no='PAYMENT-001', amount=Decimal('99.99'), currency='CNY', payment_method='wechat')
        PaymentTransactionCreate_call_mock = mocker.patch("app.modules.payment_service.service.PaymentTransactionCreate")
        PaymentTransactionCreate_call_mock.return_value = _make_namespace(payment_no='PAYMENT-001', amount=Decimal('99.99'), currency='CNY', payment_method='wechat')
        repository = context.get("repository") or getattr(service, "repository", None) or _pick_dependency(context, "repository")
        if repository:
            payment_stub = _make_namespace(amount=Decimal("99.99"), currency="value", id=1, order_id=1, payment_no="value", user_id=1)
            repository.get_by_payment_no.return_value = payment_stub
            repository.ensure_callback_idempotent.return_value = True
            repository.mark_callback_processed.return_value = None
            repository.append_transaction.return_value = None
            repository.enqueue_event.return_value = None
        _transaction_scope__transaction_scope_patch_target = service_cls if hasattr(service_cls, "_transaction_scope") else service
        _transaction_scope__transaction_scope_mock = mocker.patch.object(_transaction_scope__transaction_scope_patch_target, "_transaction_scope")
        uuid_uuid4_mock = mocker.patch("uuid.uuid4")
        uuid_uuid4_mock.return_value = SimpleNamespace(hex="FAKEUUID000000000000000000000000")
        callback = _make_namespace(callback_data="value", out_trade_no="value", transaction_id="value")
        headers = {'Wechatpay-Signature': 'signature'}
        request_client = None
        result = service.process_wechat_callback(callback=callback, headers=headers, request_client=request_client)
        assert result is not None
        if repository:
            repository.append_transaction.assert_called()
            repository.enqueue_event.assert_called()
            repository.ensure_callback_idempotent.assert_called()
            repository.get_by_payment_no.assert_called()
            repository.mark_callback_processed.assert_called()
        _transaction_scope__transaction_scope_mock.assert_called()
        uuid_uuid4_mock.assert_called()

    def test_admin_update_payment_status_success(self, mocker: MockerFixture, service_context: dict) -> None:
        """Exercise admin_update_payment_status success path."""
        if not SERVICE_AVAILABLE:
            pytest.skip("service import failed")
        context = service_context
        service = context["service"]
        service_cls = service.__class__
        create_payment_audit_log_call_mock = mocker.patch("app.modules.payment_service.service.create_payment_audit_log")
        create_payment_audit_log_call_mock.return_value = _make_namespace(payment_no='PAYMENT-001', amount=Decimal('99.99'), currency='CNY', payment_method='wechat')
        repository = context.get("repository") or getattr(service, "repository", None) or _pick_dependency(context, "repository")
        if repository:
            payment_stub = _make_namespace(callback_data="value")
            repository.get_by_id.return_value = payment_stub
            repository.update_status.return_value = None
        _transaction_scope__transaction_scope_patch_target = service_cls if hasattr(service_cls, "_transaction_scope") else service
        _transaction_scope__transaction_scope_mock = mocker.patch.object(_transaction_scope__transaction_scope_patch_target, "_transaction_scope")
        json_dumps_mock = mocker.patch("json.dumps")
        json_dumps_mock.return_value = None
        payment_id = None
        payload = _make_namespace(callback_data="value", external_payment_id="value", external_transaction_id="value", status="pending")
        admin_user = _make_namespace(id=1)
        result = service.admin_update_payment_status(payment_id=payment_id, payload=payload, admin_user=admin_user)
        assert result is not None
        if repository:
            repository.get_by_id.assert_called()
            repository.update_status.assert_called()
        _transaction_scope__transaction_scope_mock.assert_called()

    def test_get_payment_statistics_success(self, mocker: MockerFixture, service_context: dict) -> None:
        """Exercise get_payment_statistics success path."""
        if not SERVICE_AVAILABLE:
            pytest.skip("service import failed")
        context = service_context
        service = context["service"]
        service_cls = service.__class__
        func_count_mock = mocker.patch("sqlalchemy.func.count")
        count_result_mock = mocker.MagicMock(name='count_result')
        func_count_mock.return_value = count_result_mock
        count_result_mock.label.return_value = count_result_mock
        count_result_mock.scalar.return_value = Decimal("0")
        count_result_mock.scalar_one.return_value = Decimal("0")
        count_result_mock.scalar_one_or_none.return_value = Decimal("0")
        count_result_mock.all.return_value = []
        func_sum_mock = mocker.patch("sqlalchemy.func.sum")
        sum_result_mock = mocker.MagicMock(name='sum_result')
        func_sum_mock.return_value = sum_result_mock
        sum_result_mock.label.return_value = sum_result_mock
        sum_result_mock.scalar.return_value = Decimal("0")
        sum_result_mock.scalar_one.return_value = Decimal("0")
        sum_result_mock.scalar_one_or_none.return_value = Decimal("0")
        sum_result_mock.all.return_value = []
        user_id = None
        start_date = None
        end_date = None
        result = service.get_payment_statistics(user_id=user_id, start_date=start_date, end_date=end_date)
        assert result is not None
        func_count_mock.assert_called()
        func_sum_mock.assert_called()

    def test_get_pending_payments_success(self, mocker: MockerFixture, service_context: dict) -> None:
        """Exercise get_pending_payments success path."""
        if not SERVICE_AVAILABLE:
            pytest.skip("service import failed")
        context = service_context
        service = context["service"]
        service_cls = service.__class__
        utcnow_call_mock = mocker.patch("app.modules.payment_service.service.datetime")
        utcnow_call_mock.utcnow.return_value = datetime.utcnow()
        timeout_minutes = 30
        result = service.get_pending_payments(timeout_minutes=timeout_minutes)
        assert result is not None

    def test_cancel_expired_payments_success(self, mocker: MockerFixture, service_context: dict) -> None:
        """Exercise cancel_expired_payments success path."""
        if not SERVICE_AVAILABLE:
            pytest.skip("service import failed")
        context = service_context
        service = context["service"]
        service_cls = service.__class__
        repository = context.get("repository") or getattr(service, "repository", None) or _pick_dependency(context, "repository")
        if repository:
            repository.update_status.return_value = None
        _transaction_scope__transaction_scope_patch_target = service_cls if hasattr(service_cls, "_transaction_scope") else service
        _transaction_scope__transaction_scope_mock = mocker.patch.object(_transaction_scope__transaction_scope_patch_target, "_transaction_scope")
        timeout_minutes = 30
        result = service.cancel_expired_payments(timeout_minutes=timeout_minutes)
        assert result is not None

